# # Backends
# BACKEND=llama            # "llama" | "ollama"
# LLAMA_URL=http://127.0.0.1:8080
# OLLAMA_URL=http://127.0.0.1:11434

# # Auth
# API_KEYS=dev-key-123,internal-abc        # CSV danh sách key
# ADMIN_KEYS=admin-key-xyz                 # cho /admin/* (dùng ở Bài 3)

# # Server
# RATE_LIMIT=60                            # req/min/IP
# ALLOW_ORIGINS=http://localhost:5173      # CORS cho Frontend của bạn
# PORT=9000

cd D:\AI_Project\Sun\Backend
@'
# Backend runtime (chọn 1 trong 2: llama | ollama)
BACKEND=llama
LLAMA_URL=http://127.0.0.1:8080
OLLAMA_URL=http://127.0.0.1:11434

# Auth
API_KEYS=dev-key-123,internal-abc

# Rate limit (requests per minute per IP)
RATE_LIMIT=60

# CORS allowlist (để frontend gọi được)
ALLOW_ORIGINS=http://localhost:5173

# Service port (gateway)
PORT=9000
'@ | Set-Content -Encoding UTF8 .env.example